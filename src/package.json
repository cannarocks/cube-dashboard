{
  "name": "cubejs-docker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cp .env dashboard-app/.env && node index.js"
  },
  "dependencies": {
    "@cubejs-backend/mysql-driver": "^0.20.0",
    "@cubejs-backend/server": "^0.20.7",
    "dotenv": "^8.2.0",
    "express-basic-auth": "1.2.0"
  }
}
